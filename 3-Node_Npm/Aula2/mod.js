// Aula de modulos 2

// exportando apenas a funcao de multiplicar
module.exports = function(x, y) {
    return x * y;
};

//A funcao nn foi criada para ser usada aqui, mas sim em outro arquivo.

//Funciona com funcoes, objetos, strings, numeros, booleanos, arrays, etc.